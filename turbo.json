{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "outputs": ["dist/**"],
      "dependsOn": ["^dev"],
      "cache": false
    },
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"],
      "cache": false
    }
  }
}
